# when handling whitelist

- use `npm run whitelist -- --check <address>` to check for wallet if whitelisted
- use `npm run whitelist -- --add <address>` to add for wallet to whitelist -- note this is only for the contract owner!

# when handling withdrawals

- use `npm run withdraw -- --address <address> --amount <amount>` to withdraw the required amount into the provided wallet address.
